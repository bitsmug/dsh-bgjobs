# bgjobs bundle patch — 把插件行插入 profile 的层栈
# （`dsh plugin --profile <name> add bgjobs` 安装后自动挂载）。
# name 必须是包根名 `bgjobs`：host 半走 exports["."]，
# client 半由 package.json 的 dsh.client 声明（client-modules
# 按 "<name>/package.json" 定位，子路径条目名会解析不到）。
- insert:
    - id: bgjobs
      name: bgjobs
